#!/bin/ash

echo

ls
#  ls -lh | awk '{
#                name=$9;
#                 for (col=10; col<=NF; ++col )
#                    name=name" "$col;
#                 printf("%s\t%s\t%s %s %s\t%s%s", $1,$5,$6,$7,$8,name,name)
#           }'

#echo
